while (fahr < upper) b = 87.98
int a = 32 * 2
while[32 ^= 0];
while[22 == 22];
int ler343$ = 34

! this is comment for this sample code which
   converts  Fahrenheit into Celcius !

function convert$ [fahr:int]
{
           return 5 * (fahr -32) / 9;
}

%%
     int    low,  high, step$;      ! declarations !

     get (low, high, step$);
     while (low  <  high )
         {  put (low);
             put (convert$ (low));
             low =  low  +  step$;
         }
