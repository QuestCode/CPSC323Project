! this is comment for this sample code which
   converts  Fahrenheit into Celcius !

   function convertx []
   {

       if (x > y)
          x = false;
       endif
       if ( y ^= z)
          x = true;
       else y = 8;
       endif
       return;
   }


   %%
        int    low, high, step;

        get (low, high, step);
        while (low <= high)
           {  put (convert(low) );
              low = low + step;
           }
